[pytest]
markers =
    slow: real-equity 4-max solves that can take tens of minutes to hours (see README's performance section). Excluded from the default run; use `pytest -m slow` to run them explicitly.
addopts = -m "not slow"
